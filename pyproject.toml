[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "sdag"
version = "0.1.0"
description = "A Rust-based DAG execution engine with Python bindings"
requires-python = ">=3.8"